package ${package};

/** Create by FreeMarker. */
public class ${class.name?cap_first} {
    /** @param str ${class.desc} */
    public static ${class.name} create(String str) {
        // TODO 自己解析
    }
}
